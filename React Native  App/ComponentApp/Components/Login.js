import React from 'react';
import {View, Text} from 'react-native';

const Login = (props) => {
  return (
    <View>
      <Text style={{fontSize: 40}}>{props.tata}</Text>
    </View>
  );
};

export default Login;
